# 目前的困境：整理

日期：2026-08-26。本檔彙整本輪研究的狀態，供決定論文走向。

---

## 一、目標與現況的落差

| 目標（依序放寬） | 現況 |
|:---|:---|
| AI 帶來**統計顯著**的正績效 | 48 條臂**無一** DSR > 0.95（最高 0.888，且該臂後半期為負） |
| AI 帶來**可辨識**的正績效 | DRL 五組對照 3 正 2 負、中位 +0.026 pp；曝險控制後 21 列全不顯著 |
| **任一方法**年化 > 2% | 達標 6 方法／13 配置，**全部 Top 1** |
| 年化 > 2% 且 `top_n ≥ 5`（常規做法） | **0 / 549**，最高 1.452%，缺口 0.548 pp |

**最可信的策略**：`Grid (NOGRP-DTW) Top1/SL0` —— Sharpe 0.468、前後半 0.585/0.305
（**唯一**前後半皆正者）、DSR 0.674（門檻 0.95）、往返 break-even 1.219%（成本 0.580%）。

---

## 二、已否證的方向（皆有機制解釋，非單純運氣不好）

### 形成期

| 方向 | 結果 | 機制 |
|:---|:---|:---|
| ML 分群（AGG/KM/HDB） | 不如「不分群」 | 分組即限制候選池 |
| ML/DL 排序（四階梯子） | 門檻二全敗 | AUC 與 precision@k 在 **k=20 交叉** |
| 模型當否決權 | capture 顯著更差 | 技巧 +0.0009 對水準位移 −0.0044（**4.7×**） |
| 三腿共整合籃子 | 差 −0.000088（$p$=0.96） | 固定 $z$ 門檻使定態性改良中性化 |
| 形成窗 504 | 全期最佳但**後半 −0.395** | 靠 2009–2013 單段撐起 |

### 交易期

| 方向 | 結果 | 機制 |
|:---|:---|:---|
| DRL 門檻選擇 | 塌陷為常數（門檻中位 2.11–2.33、Spearman≈0） | 雜訊大時最安全的預測是常數 |
| **監督式學動作**（正確範式） | 比擴張窗常數**顯著更差**（$p$=0.0004） | 弱訊號下 argmax 放大雜訊 |
| Kalman 時變 $\beta$ | 追蹤誤差 −27.4%，年化 **−0.601 pp** | 適應把要交易的訊號吸收掉 |
| Regime 條件曝險 | ROIC ×2.5，總權益年化 **+0.029 pp** | 無槓桿的曝險縮減對 Sharpe 中性 |
| 交易窗 63 日 | 1/15 格勝出 | 重疊期數減半 → 組合波動上升 |
| OHLCV 動態出場 | 六個門檻全部更差 | 砍掉的部位回來的機率較高 |

### 組合層

| 方向 | 結果 |
|:---|:---|
| 重疊組合成本淨額化 | 可省僅 0.8%（$48） |
| 資金利用率加碼 | Sharpe +34% 是**當期正規化的前視假象**；落後 1 日即 0.297 < 0.346 |
| 投資組合合併 | 0.199–0.219 對單一 0.468 |

---

## 三、六條代數／機制障礙（這才是核心困境）

失敗不是零散的。**六條可外推的障礙互相涵蓋了絕大多數 AI 介入路徑**：

1. **$\mathrm{SSD} = 2(T-1)(1-\rho)$，$\sigma_s^2 = 1-\rho^2$**
   → SSD 排序、相關係數排序、價差波動排序是**同一個排序**（實測逐位相同）。
   文獻常見的「距離法 vs 相關係數法」對照在本實作下不構成兩個方法。

2. **AUC 與 precision@k 在 k=20 交叉**
   → 全域指標對 top-N 選取會誤導。ADF 統計量是**不含模型**的獨立實例
   （全域 AUC 0.6153 > SSD 0.5752，選 top-20 卻顯著更差，$p$=4.36e-4）。

3. **$\text{capture} = (|z_{in}|-|z_{out}|)\times\text{unit}$，而 $\text{unit}\propto\sigma_s$**
   → 進場門檻固定為 $|z|>2$ ⟹ **任何「收緊價差」的改良都經濟中性**。
   一次涵蓋 ADF 篩選、共整合排序、多腿籃子、Kalman 價差重估。

4. **無槓桿的曝險改變對 Sharpe 中性**
   → 涵蓋 regime 擇時、利用率加碼、交易窗縮短。報酬與波動同比例變動。

5. **水準位移主導模型技巧（4.7 倍）**
   → 任何「擴大候選集再重選」的方法，須先克服擴大後候選集的水準位移。
   SSD 頂端過陡（名次 21–40 的 capture 僅為 1–20 的 1/5）。

6. **以當期橫斷面總量正規化當期損益 = 內建前視**
   → $\mathrm{PnL}_t/\mathrm{occ}_t$ 的形式。驗證法：換成任何落後版本，效果消失即為同期恆等式。

**綜合起來：AI 可以介入的每一層，都各有一條障礙擋著，而且障礙是代數的不是統計的。**

---

## 四、途中發現的量測問題（已修，但顯示既有結果的脆弱性）

| 問題 | 影響 |
|:---|:---|
| break-even 名目額漏 ×6 並行因子、事件數漏強平與停損 | 10 條主力臂的 BE 全數低估 0.20–0.40 pp |
| `TRIAL_CENSUS` 釘死於 44，實地已 53 | 門檻 $SR_0$ 0.328 → 0.381、DSR 0.769 → 0.674 |
| dashboard 方法名解析 34% 落入 Unknown | 新臂全被壓成 DTW/SSD |
| `entry_z` 掃描宣告 `[1.5,2.0,2.5,3.0]` 但從未執行 | 「2.2 見頂」只在 2.0–2.3 窄區間成立 |
| `_adf_stat_batch` 在 NumPy ≥ 2.0 直接 ValueError | 既有結果不受影響（venv 為 1.26），但不可攜 |
| `vol_gate_pctl` 缺檔名後綴 | 設於既有方法上會覆寫其結果（已補） |

---

## 五、與文獻的關係：一個尚未被利用的位置

文獻中 AI 於配對交易的成功案例，其資料頻率高度一致：

- [SAPT](https://pmc.ncbi.nlm.nih.gov/articles/PMC8369334/)：小波 CNN+LSTM 預測結構性斷裂 → **分鐘級**，台灣前 150 大
- [HDRL-Trader](https://www.mdpi.com/1454446)：雙網路（動作 + 停損邊界），平均報酬 82.4% → **日內**
- Kim & Kim (2019)：DRL 同時最適化進場門檻與停損邊界

**本研究是日頻。** 而 SAPT 所預測的「結構性斷裂」，正是本研究定義並測過的
`diverged`（續發散）標籤——**日頻 S&P 500 上，模型對該標籤的 AUC 為 0.5286，
輸給 SSD 自己的 0.5393**。

> **可用的主張：文獻報告的 AI 優勢是頻率相依的。**
> 本研究提供日頻的受控對照與六條失敗機制。
> 且 82.4% 這一量級在日頻扣真實成本後不可能出現——本研究 912 個配置最高 4.5%/年。

---

## 六、仍然開放的項目

| 項目 | 狀態 |
|:---|:---|
| `entry_z ∈ {2.5, 3.0}` | **執行中**（4 臂 × 30 格）。代理說擴張窗最佳常數比現行高 50% |
| `dynamic_stop_z ∈ {3,4,5}` | 預先註冊已定稿，待跑。**文獻用的停損形式**，本研究從未啟用 |
| `max_holding_days ∈ {21,42,63}` | 同上。有既有診斷支持，但 `dyn_exit` 有反面證據，本案即為裁決 |
| `dynamic_slots` + `pair_cap_frac ≥ 0.20` | 未跑。純槓桿，Sharpe 不會改善 |
| 換資料（台股／日內／加密） | 未做。SEC 8-K 天花板已算過 0.81% |

---

## 七、真正的選擇

**目標「AI 帶來正績效」在這份資料上，我認為不存在。** 依據是六條代數障礙，
不是「還沒找到」。

三條路：

1. **換資料** —— 文獻的成功都在日內。轉頻率是唯一與文獻對齊的方向，
   但等於重建整條管線。
2. **換主張** —— 以「AI 在配對交易中的邊界條件」為主軸，
   六條機制 + DL-THR/RL-THR 之辨 + 頻率相依的文獻定位。**主軸仍是 AI。**
3. **降低標準** —— 已在 2026-08-26 討論並否決：網格最高值（FW504，Sharpe 0.608）
   後半期為 −0.395，寫成結論會是方向相反的陳述。
