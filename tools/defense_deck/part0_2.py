"""開場、第一部分（背景）、第二部分（研究問題）。"""
import math

from common import PARTS, agenda_rows, divider, reg, std
from lib import (ACCENT, BLUE, GREY, LEFT, LIGHT, NAVY, TEXT, WHITE, box,
                 callout, dot, line_seg, page_number, polyline, refs, stat,
                 table, text, arrow, CONTENT_W)

REF_GGR = ("Gatev, E., Goetzmann, W. N., & Rouwenhorst, K. G. (2006). Pairs trading: "
           "Performance of a relative-value arbitrage rule. The Review of Financial "
           "Studies, 19(3), 797–827.")
REF_DF10 = ("Do, B., & Faff, R. (2010). Does simple pairs trading still work? "
            "Financial Analysts Journal, 66(4), 83–95.")
REF_DF12 = ("Do, B., & Faff, R. (2012). Are pairs trading profits robust to trading "
            "costs? Journal of Financial Research, 35(2), 261–287.")
REF_KRAUSS = ("Krauss, C. (2017). Statistical arbitrage pairs trading strategies: "
              "Review and outlook. Journal of Economic Surveys, 31(2), 513–545.")
REF_HAN = ("Han, C., He, Z., & Toh, A. J. W. (2023). Pairs trading via unsupervised "
           "learning. European Journal of Operational Research, 307(2), 929–947.")
REF_SH = ("Sarmento, S. M., & Horta, N. (2020). Enhancing a pairs trading strategy "
          "with the application of machine learning. Expert Systems with "
          "Applications, 158, 113490.")
REF_KK = ("Kim, T., & Kim, H. Y. (2019). Optimizing the pairs-trading strategy using "
          "deep reinforcement learning with trading and stop-loss boundaries. "
          "Complexity, 2019, 3582516.")
REF_SUN = ("Sun, Y. (2025). A survey of statistical arbitrage pair trading with "
           "machine learning, deep learning, and reinforcement learning methods "
           "(Working Papers No. 22/2025). University of Warsaw.")


# ════════════════════════════════════════════════════════════════════
# 開場
# ════════════════════════════════════════════════════════════════════

@reg(0.5)
def cover(d):
    s = d.slide(dark=True)
    text(s, 1.10, 1.85, 11.1, 2.4, [
        {"text": "結合配對來源組裝與動作空間設計之", "size": 30, "bold": True,
         "color": WHITE, "after": 4},
        {"text": "配對交易系統建構與實證", "size": 40, "bold": True,
         "color": WHITE, "after": 8},
        {"text": "以 S&P 500 為例", "size": 24, "color": LIGHT},
    ])
    text(s, 1.10, 5.15, 11.1, 0.9, [
        {"text": "元智大學　資訊管理在職碩士專班", "size": 15, "color": LIGHT,
         "after": 6},
        {"text": "研究生：李伯修　　指導教授：李詩政 博士", "size": 16,
         "bold": True, "color": WHITE},
    ])
    return """
各位老師好，我是李伯修。今天要報告的論文題目是「結合配對來源組裝與動作空間設計之配對交易系統建構與實證」。

題目有點長，我先用一句白話講：這篇在問的是——如果我們用機器學習來決定配對交易什麼時候進場、什麼時候出場，那「讓模型每一期可以做幾次決定」這件事，到底重不重要？

今天大概會講一個小時，我會從配對交易是什麼開始講，所以不熟這個領域也沒關係。
"""


@reg(1.0)
def roadmap(d):
    s = std(d, "開場", "今天的路線圖")
    agenda_rows(s, current=None, y=1.85, dark=False, row_h=0.66)
    text(s, LEFT, 6.05, CONTENT_W, 0.5,
         "第四部分是重點：三段證據，而且三段的證據強度不一樣，我會一段一段分開講。",
         size=15, color=GREY)
    return """
先跟大家講一下今天的路線。

一共六個部分。第一部分我會先花幾分鐘講配對交易本身——它在做什麼、為什麼大家覺得它越來越不靈。第二部分講我想問的問題，也就是我在文獻裡看到的一個缺口。第三部分是方法，我怎麼把這個問題變成一個可以做的實驗。

第四部分是重點，也是篇幅最大的地方：三段實證結果。這邊我要先打個預防針——三段的證據強度不一樣，有的是事前就把判準寫死的，有的是分析時才採用的，我會分開講，不會混在一起。

第五部分回答一個大家一定會問的問題：所以這個東西可以拿來賺錢嗎？答案是不行，我會講清楚為什麼。最後是結論。
"""


@reg(1.0)
def conclusion_first(d):
    s = std(d, "開場", "先講結論：三個問題，三個不一樣的答案")
    rows = [
        ("Q1", "讓模型「天天決定持倉」會不會比較差？", "會，而且差很多", ACCENT,
         "換手率 2.1–4.1 倍，績效全部比較差"),
        ("Q2", "模型能不能看出「這一組配對該用哪個門檻」？", "不能", NAVY,
         "還輸給一個完全不看配對的常數"),
        ("Q3", "「每期只選一次門檻」比固定門檻好嗎？", "好，但不夠賺", NAVY,
         "五個來源方向全正，但跨不過可交易的門檻"),
    ]
    y = 1.95
    for q, ask, ans, col, why in rows:
        text(s, LEFT, y, 0.9, 0.6, q, size=26, bold=True, color=NAVY)
        text(s, 1.65, y + 0.04, 6.2, 0.9, [ask, {"text": why, "size": 13,
                                                "color": GREY}], size=17)
        text(s, 8.2, y + 0.02, 4.4, 0.6, ans, size=24, bold=True, color=col)
        y += 1.25
    callout(s, LEFT, 5.60, CONTENT_W, 0.98, None,
            [{"text": "一句話：**決定成敗的是「讓模型做幾次決定」這個設計，"
                      "不是模型聰不聰明**——但這個設計帶來的好處，還不夠大到可以拿去交易。",
              "size": 15}], style="plain")
    return """
我習慣先把答案放在前面，這樣等一下聽的時候比較不會迷路。

我問了三個問題。第一個：如果讓模型每天自由決定要不要持倉，會不會比較差？答案是會，而且差很多——換手率是其他做法的兩到四倍，績效全部比較差。

第二個：模型能不能看出「這一組配對應該用比較高的門檻、那一組應該用比較低的」？答案是不能，它甚至輸給一個完全不看配對、只用一個常數的做法。

第三個：那如果只讓模型每一期選一次門檻，會不會比傳統的固定門檻好？答案是好，五個配對來源方向全部是正的——但是好的幅度不夠大，跨不過「可以拿去交易」的門檻。

所以整篇的一句話是：決定成敗的是「讓模型做幾次決定」這個設計，而不是模型本身聰不聰明。接下來我就一步一步講我是怎麼得到這三個答案的。
"""


# ════════════════════════════════════════════════════════════════════
# 第一部分：背景
# ════════════════════════════════════════════════════════════════════

@reg(0.25, part=0)
def div_background(d):
    divider(d, 0, "先花幾分鐘講清楚：配對交易在做什麼，以及為什麼大家覺得它越來越不靈。")
    return """
好，我們進入第一部分：背景。
"""


def _spread_path():
    """示意用的價差 z 路徑（決定性、非真實資料）。"""
    ctrl = [(0, 0.1), (12, 0.3), (28, 2.25), (36, 2.6), (44, 1.6), (56, -0.05),
            (66, 0.35), (76, -0.4), (92, -2.2), (104, -2.7), (116, -3.1),
            (126, -3.35)]
    zs = []
    for t in range(127):
        for (t0, z0), (t1, z1) in zip(ctrl, ctrl[1:]):
            if t0 <= t <= t1:
                u = (t - t0) / (t1 - t0)
                u = (1 - math.cos(math.pi * u)) / 2
                base = z0 + (z1 - z0) * u
                break
        noise = 0.16 * math.sin(t * 0.9) + 0.10 * math.sin(t * 2.3 + 1.0) \
            + 0.07 * math.sin(t * 4.1 + 0.5)
        zs.append(base + noise)
    return zs


@reg(1.5, part=0)
def what_is_pairs(d):
    s = std(d, "第一部分 · 背景　｜　§1.1、§2.1.1",
            "配對交易：不猜漲跌，只賭兩檔股票會「回到原本的關係」")
    text(s, LEFT, 1.85, 5.6, 4.2, [
        "找兩檔**平常走勢很像**的股票——通常是同一批經濟因素在推動它們。",
        "平常它們的價差在一個範圍裡晃。價差**突然拉很開**時，就"
        "**買相對便宜的、賣相對貴的**。",
        "等價差回到原本的位置，兩邊一起平倉；賺的是**價差收斂**的那一段。",
        "一多一空、金額相當 → **不需要猜大盤會漲還是會跌**。",
    ], size=17, space_after=12)

    # 示意圖：價差 z 路徑
    x0, y0, w, h = 6.75, 1.95, 5.85, 3.85
    zmin, zmax = -4.0, 4.0

    def X(t):
        return x0 + w * t / 126

    def Y(z):
        return y0 + h * (zmax - z) / (zmax - zmin)

    box(s, x0 - 0.05, y0 - 0.35, w + 0.10, h + 0.75, None, fill="F7F9FC",
        shape="rect")
    text(s, x0, y0 - 0.30, w, 0.3, "價差（z 分數）示意圖", size=12, bold=True,
         color=NAVY)
    for z, lab, dash, col in [(2, "+2σ", True, ACCENT), (0, "0", False, GREY),
                              (-2, "−2σ", True, ACCENT)]:
        line_seg(s, X(0), Y(z), X(126), Y(z), color=col, width=1.0, dash=dash)
        text(s, x0 - 0.62, Y(z) - 0.13, 0.5, 0.3, lab, size=11, color=col,
             align="r")
    zs = _spread_path()
    polyline(s, [(X(t), Y(z)) for t, z in enumerate(zs)], color=BLUE, width=2.25)

    def first_cross(start, cond):
        for t in range(start, 126):
            if cond(zs[t]):
                return t
        return None

    e1 = first_cross(0, lambda z: z > 2)
    x1 = first_cross(e1, lambda z: z <= 0)
    e2 = first_cross(x1, lambda z: z < -2)
    fc = 126
    marks = [
        (e1, "① 拉開 → 進場", -0.05, -0.62),
        (x1, "② 回到 0 → 平倉獲利", -0.9, 0.18),
        (e2, "③ 又拉開 → 進場", -2.05, 0.05),
        (fc, "④ 期末還沒回來 → 強制平倉", -2.55, 0.25),
    ]
    for t, lab, dx, dy in marks:
        dot(s, X(t), Y(zs[t]), d=0.15, color=ACCENT)
        text(s, X(t) + dx, Y(zs[t]) + dy, 2.6, 0.3, lab, size=11.5, bold=True,
             color=TEXT)
    text(s, x0, y0 + h + 0.08, w, 0.3, "模擬路徑，僅供說明，非實際資料",
         size=10, color=GREY, align="r")
    refs(s, [REF_GGR])
    return """
配對交易的邏輯其實很直覺。

我們找兩檔平常走勢很像的股票——通常是因為同一批經濟因素在推動它們，比如同產業的兩家公司。平常它們的價差會在一個範圍裡面晃來晃去。

右邊這張是示意圖，是我畫的模擬路徑，不是真實資料。縱軸是價差，換算成 z 分數。當價差突然拉很開、超過正兩個標準差，也就是第一個點，我們就買相對便宜的那一檔、賣相對貴的那一檔。等價差回到零，第二個點，兩邊一起平倉，賺的就是價差收斂的那一段。

請大家特別看第三、第四個點：價差又拉開，我們又進場，但它一直沒有回來，到交易期結束還在發散，這時候不管怎樣都要強制平倉——這種情況通常是虧的。這個「期末強制平倉」等一下在第五部分會變得非常重要。

最後，因為是一多一空、金額差不多，所以我們不需要猜大盤會漲還是會跌。這就是它被叫做「市場中性」策略的原因。
"""


@reg(1.25, part=0)
def ggr_rules(d):
    s = std(d, "第一部分 · 背景　｜　§2.1.2、§3.1.3",
            "標準玩法：先花一年挑配對，再花半年照規則交易")
    # 時間軸
    y = 1.95
    box(s, LEFT, y, 6.9, 1.05, [
        {"text": "形成期　252 個交易日（約一年）", "size": 16, "bold": True,
         "color": WHITE, "after": 2},
        {"text": "挑配對、估參數——這段時間不交易", "size": 13, "color": LIGHT}],
        fill=NAVY, anchor="m")
    box(s, 7.75, y, 4.85, 1.05, [
        {"text": "交易期　126 個交易日（約半年）", "size": 16, "bold": True,
         "color": WHITE, "after": 2},
        {"text": "照規則進出——完全樣本外", "size": 13, "color": "FBE3DC"}],
        fill=ACCENT, anchor="m")
    arrow(s, 7.62, y + 0.52, 7.74, y + 0.52, color=GREY)
    table(s, LEFT, 3.35, [3.0, 8.9], [
        ["規則", "作法"],
        ["怎麼挑配對", "兩檔股票標準化價格的距離最小（SSD）的前幾組"],
        ["什麼時候進場", "價差偏離形成期標準差 **2 倍**時，做多或做空價差"],
        ["什麼時候出場", "價差**回到 0** 時平倉"],
        ["期末怎麼辦", "交易期結束，**不管回來沒有，一律強制平倉**"],
    ], size=15, row_h=0.40)
    text(s, LEFT, 5.60, CONTENT_W, 0.7, [
        "這套規則出自 Gatev、Goetzmann 與 Rouwenhorst（2006，以下簡稱 GGR）。"
        "後面我講的「**Z-Score 基準**」就是它：進場 2.0、出場 0.0，固定不變。"],
        size=15, color=GREY)
    refs(s, [REF_GGR])
    return """
配對交易最標準的玩法，是 2006 年 GGR 這篇論文定下來的，到今天幾乎所有研究都拿它當基準。

流程分兩段。第一段叫形成期，252 個交易日，差不多一年，這段時間只做一件事：挑配對、估參數，完全不交易。第二段叫交易期，126 個交易日，差不多半年，就照規則進出場。兩段不重疊，所以交易期是完全樣本外的，不會有偷看未來的問題。

規則很簡單：挑距離最小的配對；價差偏離兩個標準差就進場；回到零就出場；然後最後一列——交易期結束的時候，不管價差回來沒有，一律強制平倉。

後面我會一直提到「Z-Score 基準」，指的就是這一套：進場門檻 2.0、出場門檻 0.0，固定不變，不做任何學習。

補充一個細節：我算距離用的是對數價格的 z 分數，跟 GGR 原文用的總報酬指數略有不同——我的版本衡量的是「走勢形狀」像不像，這樣做是為了跟交易端的價差計算放在同一個標準化空間裡。
"""


@reg(0.75, part=0)
def why_profit(d):
    s = std(d, "第一部分 · 背景　｜　§2.1.1",
            "為什麼這樣會賺錢？因為它賭的是「相對」價格")
    cards = [
        ("① 賭相對，不賭絕對",
         "不預測任何一檔會漲會跌，只賭「有共同驅動因子的兩檔，相對價格會回到均衡附近」"
         "——可以想成一價法則的弱化版。"),
        ("② 市場中性",
         "一多一空、金額相當。GGR 實證它對 Fama-French 三因子的曝險很低，"
         "所以報酬很難用既有的風險因子解釋。"),
        ("③ 不一定要同產業",
         "同產業只是共同衝擊的**充分、而非必要**條件。跨產業但受同樣總體因子驅動的兩檔，"
         "理論上也能配對——這正是後來機器學習分群想挖的東西。"),
    ]
    x = LEFT
    for head, body in cards:
        callout(s, x, 1.95, 3.8, 2.75, head, [body], style="plain", size=15,
                heading_size=17)
        x += 4.05
    text(s, LEFT, 5.00, CONTENT_W, 0.6,
         "記住第三點：它是「用機器學習找配對」這條路線的理論依據。", size=15,
         color=GREY)
    refs(s, [REF_GGR])
    return """
那為什麼這樣會賺錢？三個理由。

第一，它賭的是相對價格，不是絕對價格。我不需要預測任何一檔股票會漲會跌，我只需要相信：兩檔有共同經濟驅動因子的股票，它們的相對價格會回到某個均衡附近。這可以想成是「一價法則」的弱化版本。

第二，它是市場中性的。GGR 當年就驗證過，配對組合對 Fama-French 三因子的曝險非常低，所以這份報酬很難用市場、規模、價值這些已知的風險因子來解釋，這也是它被叫做「異常報酬」的原因。

第三點很重要：它其實不要求兩檔同產業。同產業只是一個充分條件，不是必要條件——兩檔不同產業、但被同樣總體因子推動的股票，理論上也可以是好配對。這就是後來大家用機器學習去做分群、找跨產業配對的理論依據。
"""


@reg(1.25, part=0)
def decay(d):
    s = std(d, "第一部分 · 背景　｜　§2.1.3–2.1.4",
            "問題來了：這招越來越不靈")
    text(s, LEFT, 1.80, CONTENT_W, 0.8,
         "GGR 在 1962–2002 年報告年化超額報酬最高可達 11%。但後續研究一致發現："
         "**2000 年代之後大幅衰減**。", size=17)
    cards = [
        ("Do & Faff（2010）", "樣本延伸到 2009 年：獲利從 1990 年代末開始顯著下降，"
                             "而且不是被某一段異常期間拉下來的。"),
        ("Do & Faff（2012）", "把佣金、買賣價差、市場衝擊算進去（單邊約 30 個基點）"
                             "之後，多數策略的淨報酬已經**不顯著**。"),
        ("Krauss（2017）", "回顧五大類方法：**方法之間的差異，常常比換一段樣本期間"
                          "造成的差異還小**。"),
    ]
    x = LEFT
    for head, body in cards:
        callout(s, x, 2.65, 3.8, 2.0, head, [body], style="plain", size=14,
                heading_size=16)
        x += 4.05
    callout(s, LEFT, 4.90, CONTENT_W, 1.3, "而且衰減在大型股上更明顯",
            ["我的母體正好是 S&P 500 大型股、期間 2000–2025——**完全落在衰減之後**。"
             "這是刻意的：我問的不是「它以前賺不賺」，而是「學習式交易端能不能在衰減之後"
             "重建優勢，以及好處從哪裡來」。"], size=14)
    refs(s, [REF_DF10, REF_DF12, REF_KRAUSS], y=6.42)
    return """
但問題來了：這招越來越不靈。

GGR 當年在 1962 到 2002 年的樣本裡，報告的年化超額報酬最高可以到 11%。但後面的研究很一致地發現，這個績效在 2000 年之後大幅衰減。

Do 跟 Faff 2010 年那篇把樣本拉到 2009 年，發現獲利從九零年代末就開始往下掉，而且不是被某一段特別差的期間拉下來的。2012 年那篇更直接：把真實的交易成本放進去，單邊大概 30 個基點，多數策略的淨報酬就不顯著了。

Krauss 2017 年的回顧有一句話我很喜歡：方法之間的差異，常常比你換一段樣本期間造成的差異還小。換句話說，你以為是方法進步，可能只是剛好換到一段比較好的期間。

下面這點對我很重要：衰減在流動性高的大型股上更明顯，而我的樣本剛好就是 S&P 500、2000 到 2025 年，完全落在衰減之後。這是我刻意的選擇——我要問的不是它以前賺不賺錢，而是學習式的交易端能不能在衰減之後把優勢找回來，以及如果有好處，好處是從哪裡來的。
"""


@reg(1.0, part=0)
def two_routes(d):
    s = std(d, "第一部分 · 背景　｜　§2.2.4、§2.3.4",
            "大家的解法：把機器學習丟進去")
    text(s, LEFT, 1.80, CONTENT_W, 0.6,
         "配對交易有兩個階段，機器學習也就被用在兩個地方。多數研究只動其中一邊。",
         size=17)
    callout(s, LEFT, 2.55, 5.75, 2.55, "① 形成期：改良「怎麼找配對」",
            ["用非監督式分群取代傳統產業分類，去找**跨產業的隱性關聯配對**。",
             {"text": "代表：Han, He & Toh（2023）、Sarmento & Horta（2020）",
              "size": 13, "color": GREY}], style="plain", size=15, heading_size=17)
    callout(s, 6.85, 2.55, 5.75, 2.55, "② 交易期：改良「什麼時候進出場」",
            ["用深度強化學習取代固定門檻，讓模型**自己決定進出場**與停損邊界。",
             {"text": "代表：Kim & Kim（2019）", "size": 13, "color": GREY}],
            style="plain", size=15, heading_size=17)
    callout(s, LEFT, 5.35, CONTENT_W, 0.95, None,
            [{"text": "我做的是 ②。但我要問的**不是**「DRL 有沒有用」——而是一個更前面、"
                      "文獻還沒有人把它拿出來問的問題。", "size": 15}], style="light")
    refs(s, [REF_HAN, REF_SH, REF_KK], y=6.45)
    return """
面對這個衰減，文獻的反應很自然：把機器學習丟進去，看能不能救回來。

配對交易本來就分兩段，所以機器學習也就被用在兩個地方。第一條路線改良形成期，也就是怎麼找配對：用非監督式分群取代 GICS 產業分類，去找跨產業、但被同樣因子推動的配對。Han、He 跟 Toh 2023 年那篇是這條路最完整的證據。

第二條路線改良交易期，也就是什麼時候進出場：用深度強化學習取代固定的兩倍標準差門檻，讓模型自己學。Kim 跟 Kim 2019 年是這條路的代表。

我做的是第二條。但我要強調，我問的不是「DRL 有沒有用」這種問題——那個已經很多人做過了，而且答案常常跟樣本期間綁在一起。我要問的是一個更前面的問題，第二部分會講。

不過在那之前，我想先分享一個我在整理文獻時看到、覺得滿有意思的現象。
"""


@reg(1.5, part=0)
def intraday_pattern(d):
    s = std(d, "第一部分 · 背景　｜　§2.4",
            "有個現象很有趣：AI 的成功案例，幾乎都是日內資料")
    table(s, LEFT, 1.85, [2.7, 4.6, 1.6, 2.2], [
        ["研究", "方法", "資料頻率", "報告績效"],
        ["SAPT（2021）＊", "小波 CNN+LSTM 預測結構性斷裂＋DQN 停損", "分鐘級", "較基準 +456%"],
        ["HDRL-Trader（2022）＊", "雙網路（動作＋停損邊界）", "日內", "平均報酬 82.4%"],
        ["Sarmento & Horta（2020）", "OPTICS 密度分群", "5 分鐘", "Sharpe 3.79"],
        ["Kim & Kim（2019）", "DRL 門檻＋停損", "日內", "優於固定門檻"],
        ["Han, He & Toh（2023）", "特徵空間分群（CRSP 全美股、126 個特徵）", "月頻", "年化超額 24.8%"],
    ], size=13, row_h=0.38, accent_cells={(1, 2), (2, 2), (3, 2), (4, 2)})
    callout(s, LEFT, 4.30, 5.75, 2.05, "兩種讀法，意涵相反",
            ["① 方法有效，只是**日頻的訊噪比撐不起它**",
             "② 績效跟頻率無關，型態來自**發表選擇**",
             {"text": "單一研究分不出來；但可以補上缺的那組「日頻對照」。",
              "size": 13, "color": GREY}], size=14)
    callout(s, 6.85, 4.30, 5.75, 2.05, "我在日頻上試了兩個構想：都沒成立",
            ["預測「價差不會回來」：AUC **0.5286**，還輸給 SSD 排序本身的 0.5393",
             "3／4／5 個標準差停損：**沒有任何一個**讓年化轉正"
             "（NOGRP-SDP：+1.452% → −0.627%／−0.873%／−0.090%）"], style="plain",
            size=13.5)
    text(s, LEFT, 6.50, CONTENT_W, 0.3,
         "＊ SAPT、HDRL-Trader 的完整書目待補（論文 §2.4 已註記）；"
         "Sarmento & Horta、Kim & Kim、Han et al. 見參考文獻。", size=9, color=GREY)
    return """
我把文獻裡「報告 AI 做配對交易有效」的研究，依照資料頻率排出來，發現一個很一致的型態：成功案例幾乎都是日內資料——分鐘級、五分鐘、日內。

唯一的例外是最下面 Han 等人 2023 年那篇，它是月頻，但它用的是整個 CRSP 全美股、數千檔股票，加上 126 個特徵，母體跟特徵都比一般研究大得多。

這個型態有兩種讀法，而且意涵剛好相反。第一種：方法本身有效，只是日頻資料的訊噪比撐不起它——那日頻失敗不代表方法不好。第二種：績效其實跟頻率無關，這個型態是發表選擇造成的——那日頻失敗就是對方法的實質否證。一篇研究沒辦法分辨這兩種，但可以補上缺的那組東西：日頻上的受控對照。

所以我把其中兩個核心構想搬到日頻 S&P 500 上試。第一個是預測「價差不會回來」，模型的 AUC 是 0.5286，還輸給它想改良的 SSD 排序本身。第二個是用標準差設停損，三個、四個、五個標準差，沒有任何一個讓年化報酬轉正。

我要誠實說明一點：表上打星號的兩篇，書目資料我還在補，論文裡也已經註記了。這張的重點是：構想不會自動從日內移轉到日頻。記住這件事，它跟我後面的主題有關。
"""


# ════════════════════════════════════════════════════════════════════
# 第二部分：研究問題
# ════════════════════════════════════════════════════════════════════

@reg(0.25, part=1)
def div_question(d):
    divider(d, 1, "文獻裡有一個大家每篇都在做、卻從來沒被拿出來檢驗的決定。")
    return """
好，第二部分：我想問的問題。
"""


@reg(1.25, part=1)
def the_gap(d):
    s = std(d, "第二部分 · 研究問題　｜　§1.2.1、§2.3.5",
            "被跳過的問題：到底該讓模型做幾次決定？")
    text(s, LEFT, 1.80, CONTENT_W, 1.2, [
        "把 DRL 做配對交易的論文攤開來看，每一篇都得先決定：**模型每一期可以做幾次決策？**",
        "多數研究給了模型「**每天自由決定持倉**」的權限——做多價差、做空價差、空手。"
        "但沒有一篇解釋為什麼，也沒有人把它單獨拿出來比。"], size=17, space_after=10)
    callout(s, LEFT, 3.20, CONTENT_W, 1.15, "動作空間（action space）＝模型可以做的決策集合",
            ["它是每一篇 DRL 論文都必須設定、卻從來沒有被當成實驗變因的設計選擇。"],
            style="dark", size=15, heading_size=18)
    text(s, LEFT, 4.60, CONTENT_W, 0.4, "沒有控制這個變因，會發生三件事：", size=16,
         bold=True, color=NAVY)
    items = [
        ("失敗時", "分不清是演算法不好，還是動作空間設計不好"),
        ("成功時", "分不清是模型真的「看懂配對」，還是選單的形狀剛好比較好"),
        ("跨論文比較", "頻率、母體都不同，取代不了同一條管線裡的消融"),
    ]
    x = LEFT
    for head, body in items:
        box(s, x, 5.05, 3.8, 1.25, [
            {"text": head, "size": 15, "bold": True, "color": ACCENT, "after": 4},
            {"text": body, "size": 14}], fill="F7F9FC", line=("D9DEE7", 1.0))
        x += 4.05
    refs(s, [REF_KK, REF_SUN], y=6.50)
    return """
這就是我看到的缺口。

當你把所有用 DRL 做配對交易的論文攤開來，會發現它們都做了同一個決定：要讓模型每一期可以做幾次決策。而多數研究給了模型很大的權限——每天自由決定持倉：做多價差、做空價差、或是空手。

但沒有任何一篇解釋為什麼是這個設計，它就像一個理所當然的預設值。

我把它叫做「動作空間」，就是模型可以做的決策集合。每一篇 DRL 論文都一定要設定它，但它從來沒有被當成實驗變因。

沒有控制它會怎樣？三件事。第一，模型失敗的時候，你分不清是演算法不好，還是動作空間設計不好。第二，模型成功的時候，你分不清是它真的看懂了每一組配對，還是只是選單的形狀剛好比較好。第三，每篇論文的頻率、母體都不一樣，跨論文比較沒辦法取代在同一條管線裡面做的消融實驗。

這就是我的切入點。
"""


@reg(1.25, part=1)
def three_spaces(d):
    s = std(d, "第二部分 · 研究問題　｜　§2.3.5、§3.3.1",
            "動作空間，說白了就是：每一期可以做幾次決定")
    cards = [
        ("0", "次", "固定門檻（不決策）",
         "進場 2.0、出場 0.0，永遠不變。", "GGR 以來的傳統研究", NAVY),
        ("1", "次", "每期選一組門檻",
         "每期開始前，從 9 個選項挑 1 個：拒絕交易（SKIP）或 8 組進出場門檻。",
         "本研究的 DL-THR", BLUE),
        ("126", "次", "逐日自由決定持倉",
         "每天都在 {做多、做空、空手} 裡選一個。", "多數 DRL 文獻", ACCENT),
    ]
    x = LEFT
    for big, unit, name, desc, who, col in cards:
        box(s, x, 1.90, 3.8, 3.35, None, fill="F7F9FC", line=("D9DEE7", 1.0))
        text(s, x + 0.25, 2.05, 3.3, 0.95, [
            {"text": f"{big}", "size": 48, "bold": True, "color": col}],
             space_after=0)
        text(s, x + 0.25 + (0.62 if len(big) == 1 else 1.55), 2.55, 1.2, 0.4,
             "次決策／期" if big != "126" else "次（最多）", size=13, color=GREY)
        text(s, x + 0.25, 3.10, 3.3, 0.4, name, size=17, bold=True, color=NAVY)
        text(s, x + 0.25, 3.55, 3.3, 1.1, desc, size=14)
        text(s, x + 0.25, 4.75, 3.3, 0.4, who, size=13, color=GREY)
        x += 4.05
    callout(s, LEFT, 5.50, CONTENT_W, 0.85, None,
            [{"text": "為什麼是 126？交易期就是 126 個交易日。天天決定，一期最多就是 126 次——"
                      "三者的決策次數**差了兩個數量級**。", "size": 15}], style="plain")
    return """
所以動作空間，說白了就是：模型每一期可以做幾次決定。我把它分成三類。

第一類是零次：固定門檻。進場 2.0、出場 0.0，永遠不變，它根本不做決定。這就是 GGR 以來的傳統做法。

第二類是一次：每一期開始之前，從九個選項裡挑一個——可以選擇這期不交易，也就是 SKIP，或是八組不同的進出場門檻。這是我的交易端，叫 DL-THR，等一下方法會細講。

第三類是最多 126 次：每天都在做多、做空、空手之間選一個。這是多數 DRL 文獻的做法。為什麼是 126？因為交易期就是 126 個交易日，天天決定，一期最多就是 126 次。

所以你看，三者的決策次數，從零、一到一百二十六，差了兩個數量級。問題是：這個差異到底重不重要？
"""


@reg(1.0, part=1)
def tension(d):
    s = std(d, "第二部分 · 研究問題　｜　§2.3.5、§2.3.6",
            "直覺說越大越好——但真的是這樣嗎？")
    callout(s, LEFT, 1.95, 5.75, 2.75, "支持「越大越好」",
            ["天天決定的動作空間，**表達能力涵蓋**任何固定門檻規則。",
             "理論上它至少不會比較差——大不了模型學出一條固定規則就好。"],
            style="plain", size=15, heading_size=18)
    callout(s, 6.85, 1.95, 5.75, 2.75, "支持「越大越糟」",
            ["決策越多次，每次決策依據的訊號越短、**雜訊佔比越高**。",
             "而且每一次決策，都可能多付一次**換手成本**。"],
            style="light", size=15, heading_size=18)
    box(s, LEFT, 5.00, CONTENT_W, 1.25, [
        {"text": "這個取捨在日頻上往哪邊倒？", "size": 20, "bold": True,
         "color": WHITE, "after": 4},
        {"text": "不需要理論爭辯——可以直接用實驗回答。文獻沒做，因為沒有人把動作空間當成變因。",
         "size": 15, "color": LIGHT}], fill=NAVY, anchor="m")
    return """
那直覺上，動作空間越大應該越好吧？

左邊是支持「越大越好」的理由：天天決定的動作空間，表達能力涵蓋任何固定門檻規則——如果固定門檻真的比較好，模型大不了就學出一條固定規則。所以理論上，它至少不會比較差。

右邊是支持「越大越糟」的理由：決策次數越多，每次決策能依據的訊號就越短，雜訊佔比就越高。更實際的是，每一次決策都可能多付一次換手成本——剛剛講過，成本在這個策略裡是非常致命的。

所以這裡有一個取捨：表達能力，對上訊噪比跟成本。在日頻上它到底往哪邊倒？這不需要理論爭辯，可以直接用實驗回答。而文獻沒有做這個實驗，就是因為沒有人把動作空間當成變因。我的研究就是去做這個實驗。
"""


@reg(1.25, part=1)
def research_questions(d):
    s = std(d, "第二部分 · 研究問題　｜　§1.2.3",
            "所以我問了三個問題")
    qs = [
        ("Q1", "動作空間是不是關鍵？",
         "其他一切都一樣，只把每期決策次數從 0、1 放大到 126——會不會系統性地過度交易、績效變差？",
         "完整預先登記"),
        ("Q2", "縮小之後，模型還剩什麼能力？",
         "每期只選一次門檻時，模型能不能看出「這組配對該用哪個門檻」——也就是贏過一個完全不看配對的常數？",
         "跑前定稿"),
        ("Q3", "跟固定門檻比有沒有用？界線在哪？",
         "門檻選擇相對固定門檻是否穩定較好；這個好處在哪種報酬口徑下成立、哪種口徑下不能說可以交易？",
         "分析時判準"),
    ]
    y = 1.85
    for q, head, body, grade in qs:
        text(s, LEFT, y, 1.0, 0.7, q, size=28, bold=True, color=NAVY)
        text(s, 1.85, y + 0.02, 8.3, 1.2, [
            {"text": head, "size": 18, "bold": True, "color": NAVY, "after": 4},
            {"text": body, "size": 14.5}])
        box(s, 10.45, y + 0.12, 2.15, 0.45, grade, fill=LIGHT, color=NAVY,
            size=12, bold=True, align="c", anchor="m", inset=(0.05, 0.02))
        y += 1.38
    text(s, LEFT, 6.05, CONTENT_W, 0.5,
         "右邊標的是每一題的**證據等級**——我會分開報告，不把它們混在一起。",
         size=15, color=GREY)
    return """
所以我問了三個問題，而且它們是一個接一個的。

Q1 是核心：動作空間到底是不是關鍵？在其他一切都一樣的條件下，只把每期決策次數從零、一放大到 126，會不會讓模型系統性地過度交易、績效變差？

Q2 是接著問的：如果 Q1 告訴我們該把動作空間縮小，那縮小之後模型還剩什麼能力？每期只選一次門檻的時候，它能不能看出「這一組配對應該用比較高的門檻」——講白了，它能不能贏過一個完全不看配對、只用一個常數的做法？

Q3 是驗收：門檻選擇相對固定門檻到底有沒有用？而且我刻意把「界線在哪」也放進題目，因為我的答案在相對層面是「有」，在絕對層面是「不能交易」，這兩件事不能混為一談。

右邊這一欄請大家記一下，它是每一題的證據等級。Q1 是完整預先登記的，判準在跑之前就寫死了；Q2 是跑之前定稿；Q3 則是分析時依照文獻慣例採用的判準。我後面報結果時，會一直提醒這個差別。
"""


@reg(1.0, part=1)
def scope(d):
    s = std(d, "第二部分 · 研究問題　｜　§1.3",
            "先講清楚：我主張什麼，不主張什麼")
    callout(s, LEFT, 1.90, 5.75, 2.55, "我會檢驗的",
            ["**動作空間**的設計，對學習式配對交易端的影響",
             "三段證據：動作空間消融 → 逐對學門檻 vs 最佳常數 → 門檻選擇 vs 固定門檻",
             "共同原則：**任兩條被比較的策略，只差一個地方**"],
            style="plain", size=14.5, heading_size=17)
    callout(s, 6.85, 1.90, 5.75, 2.55, "我不主張的",
            ["✘ 這個交易端**可以實際獲利**",
             "✘ 資料驅動分群**優於**產業分組（在我的資料條件下沒被支持，只做描述）",
             "✘ 任何關於**日內頻率**的結論"],
            style="light", size=14.5, heading_size=17)
    text(s, LEFT, 4.70, CONTENT_W, 0.4, "另外給自己三個方法上的要求：", size=16,
         bold=True, color=NAVY)
    goals = [
        ("推論基礎要對", "不拿各自網格的最佳值比較；一律用全網格等權組合，"
                        "檢定同時報 p 值與信賴區間"),
        ("判準先於資料", "消融的假說、門檻、核對程序，在回測開跑前就提交版本庫"),
        ("限制要量化揭露", "存活者偏誤、覆蓋率、成本年代，以及開發中發現的實作錯誤，都用數字交代"),
    ]
    x = LEFT
    for head, body in goals:
        box(s, x, 5.15, 3.8, 1.2, [
            {"text": head, "size": 15, "bold": True, "color": NAVY, "after": 4},
            {"text": body, "size": 13}], fill="F7F9FC", line=("D9DEE7", 1.0))
        x += 4.05
    return """
在進方法之前，我想先把範圍講清楚：我主張什麼、不主張什麼。

我會檢驗的，是動作空間的設計對學習式交易端的影響，透過剛剛那三段證據。三段共用一個原則：任兩條被拿來比較的策略，只能差一個地方。

我不主張的有三件事。第一，我不主張這個交易端可以實際獲利——第五部分會講為什麼。第二，我不主張資料驅動的分群比產業分組好——這在我的資料條件下沒有被支持，我只做描述。第三，我不涉及日內頻率。

另外，我給自己三個方法上的要求。第一，推論基礎要對：不拿每條策略各自網格裡最好的那一格來比，一律用全網格等權組合。第二，判準要先於資料：消融的假說、門檻、核對程序，在回測開跑之前就提交到版本庫。第三，限制要量化揭露——包括我自己在開發過程中犯的實作錯誤，我都會用數字交代，不會因為修好了就不講。
"""
