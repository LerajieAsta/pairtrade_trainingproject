"""第四部分：結果（三段證據、增益來源、穩健性）。"""
from common import divider, reg, std
from lib import (ACCENT, BLUE, GREY, LEFT, LIGHT, NAVY, TEXT, WHITE, box,
                 callout, bar_chart, line_chart, refs, stat, table, text, CONTENT_W)

# 圖表數據沿用 thesis/1150922.pptx 已核對的版本（換手率取 ablation_h1_turnover.csv 的
# 108 期共同期狀態列式；權益曲線為 daily_returns_mainaxis.parquet 15 格等權重建）。
TURNOVER_CATS = ["Z-Score\n（0 次決策）", "DL-THR\n（1 次決策）", "v3 FQI\n（≤126 次）",
                 "v1 DQN\n（≤126 次）", "v2 DQN\n（≤126 次）"]
TURNOVER_VALS = [1.962962962962963, 1.9444444444444444, 4.203703703703703,
                 5.592592592592593, 7.907407407407407]
YEARS = [str(y) for y in range(2001, 2026)]
EQ_DL = [9918, 10179, 10135, 10141, 10311, 10679, 11116, 13340, 14344, 14241, 13916,
         14090, 13926, 13795, 13694, 13783, 13684, 13530, 13353, 13416, 13406, 13396,
         13442, 13021, 13062]
EQ_ZS = [10023, 10293, 10210, 10123, 10219, 10588, 11081, 12088, 12796, 12926, 12570,
         12697, 12478, 12284, 12189, 12214, 12097, 11888, 11615, 11691, 11630, 11638,
         11590, 11047, 11072]

L1 = "第四部分 · 第一段（完整預先登記）　｜　"
L2 = "第四部分 · 第二段（跑前定稿）　｜　"
L3 = "第四部分 · 第三段（分析時判準）　｜　"
LX = "第四部分 · 增益來源與穩健性（分析時判準）　｜　"


@reg(0.5, part=3)
def div_results(d):
    s = divider(d, 3, "三段證據依序回答三個問題——前兩段是否證，第三段是正面結果。")
    return """
好，進入第四部分，結果。

提醒一下結構：三段證據依序回答三個問題。前兩段的結果是否證——也就是「不是大家以為的那樣」；第三段是正面結果。然後我會花一些時間檢查第三段的正面結果到底是不是真的，最後是穩健性。

每一張的頁首我都會標出證據等級，第一段是完整預先登記、第二段是跑前定稿、第三段之後是分析時判準。
"""


@reg(1.5, part=3)
def h1_turnover(d):
    s = std(d, L1 + "§4.1.2", "第一段：讓模型天天決定，換手率多出 2 到 4 倍")
    bar_chart(s, LEFT, 1.80, 7.3, 4.45, TURNOVER_CATS, TURNOVER_VALS,
              [BLUE, BLUE, ACCENT, ACCENT, ACCENT], number_format="0.00",
              val_min=0, val_max=9)
    text(s, LEFT, 6.25, 7.3, 0.3, "每期進場次數（108 期共同期；反向也算一次進場）",
         size=11, color=GREY, align="c")
    table(s, 8.25, 1.95, [1.45, 1.05, 1.05, 0.85], [
        ["處理臂", "對 Z-Score", "對 DL-THR", "BH p"],
        ["v1", "2.849", "2.876", "<.0001"],
        ["v2", "4.028", "4.067", "<.0001"],
        ["v3", "2.142", "2.162", ".0004"],
    ], num_cols={1, 2, 3}, size=13, row_h=0.38, header_size=12)
    text(s, 8.25, 3.50, 4.4, 0.3, "表中為總進場數比值", size=11, color=GREY)
    callout(s, 8.25, 3.82, 4.35, 2.55, "3 / 3 達標",
            ["判準只要求 2/3。三條臂**全部同時贏過兩個基準**。",
             "n = 108 期、區塊 6 期 → 只有 **18 個獨立區塊**，這是檢定力的實際上限。",
             {"text": "換一種進場定義重算，比值 2.868–4.095，結論不變。", "size": 12,
              "color": "C9D3E3"}], style="dark", size=12.5, heading_size=20)
    return """
第一段結果。這是我唯一完整預先登記的部分，也是證據最強的一段。

左邊長條圖，橫軸是五條臂，縱軸是每期進場次數。藍色兩條是基準：固定門檻 1.96 次、DL-THR 1.94 次，幾乎一樣。紅色三條是天天決定持倉的：v3 是 4.2 次、v1 是 5.6 次、v2 是 7.9 次。

右上的表是總進場數的比值：對 Z-Score 是 2.1 到 4.0 倍，對 DL-THR 也差不多，BH 校正後全部顯著，最大的 p 值也只有萬分之四。判準只要求三條裡面兩條達標，結果是三條全部達標，而且每一條都同時贏過兩個基準。

右下我要誠實講一件事：雖然有 108 期，但因為相鄰期重疊，區塊長度要取 6 期，所以實際上只有 18 個獨立區塊。這是這個設計檢定力的上限，我不會宣稱比這更多的東西。

另外，我也換了一種進場的定義重算，比值 2.87 到 4.10，結論不變。所以答案很清楚：讓模型天天決定，它就會交易得多很多。
"""


@reg(1.0, part=3)
def h2_perf(d):
    s = std(d, L1 + "§4.1.3", "而且績效全部比較差")
    table(s, LEFT, 1.90, [2.1, 1.45, 1.45, 1.45, 1.75], [
        ["臂", "交集 Sharpe", "交集年化%", "交集 MDD%", "各自全期 Sharpe"],
        ["Z-Score", "−0.009", "−0.24", "−16.51", "+0.418"],
        ["DL-THR（v4）", "−0.012", "−0.26", "−16.54", "+0.442"],
        ["v1", "−0.829", "−4.12", "−34.64", "−0.829"],
        ["v2", "−1.547", "−5.46", "−41.73", "−1.247"],
        ["v3", "−0.298", "−1.75", "−18.79", "+0.045"],
    ], num_cols={1, 2, 3, 4}, size=14, row_h=0.42,
        bold_cells={(3, 1), (4, 1), (5, 1)})
    text(s, LEFT, 4.60, 8.2, 0.5,
         "H2 成立：三條臂的風險調整後報酬，全部不高於門檻選擇。", size=16, bold=True,
         color=NAVY)
    callout(s, 9.10, 1.90, 3.50, 4.40, "一個一定要並列的口徑事實",
            ["取五臂交集，會丟掉左緣 11 期——剛好是 **2008-07～2009-06 的危機反彈**。",
             "兩個基準的獲利幾乎都在那裡：只看左緣期，Sharpe 是 **+1.443／+1.541**。",
             "所以交集上基準≈0，**不代表**它們全樣本也是 0。組內比較是公平的，但不能拿來說基準也不行。"],
            style="light", size=13)
    return """
H2 是績效。表上的「交集 Sharpe」是五臂共同 108 期上重算的。三條天天決定的臂：v1 負 0.83、v2 負 1.55、v3 負 0.30；兩條基準都在負 0.01 附近。所以 H2 成立：三條臂的風險調整後報酬，全部不高於門檻選擇。

但右邊這件事我一定要並列講，不然會誤導大家。你會發現兩個基準在交集上的 Sharpe 也是負的——這是因為取五臂交集的時候，丟掉了左緣 11 期，而那剛好是 2008 年 7 月到 2009 年 6 月的危機反彈期。兩個基準的獲利幾乎都在那段裡面：只看那 11 期，它們的 Sharpe 是正 1.44 跟 1.54。

所以表格最右邊那欄，是各自在全部期間上的 Sharpe，基準是正 0.42、0.44。組內比較是公平的，因為五臂用的是同一組 108 期；但我不會拿交集的數字去說基準也不行。這一段講的是相對的行為差異，不是絕對績效。
"""


@reg(1.25, part=3)
def h3_cost(d):
    s = std(d, L1 + "§4.1.4–4.1.5", "但三代輸的原因不一樣")
    table(s, LEFT, 1.90, [1.1, 2.0, 2.0, 1.5, 0.9], [
        ["臂", "含成本 ΔSharpe", "零成本 ΔSharpe", "差距縮小", "H3"],
        ["v1", "−0.816", "−0.106", "87.0%", "✔"],
        ["v2", "−1.535", "−0.255", "83.4%", "✔"],
        ["v3", "−0.285", "+0.195", "31.7%", "✘"],
    ], num_cols={1, 2, 3}, center_cols={4}, size=15, row_h=0.46,
        bold_cells={(1, 3), (2, 3), (3, 2)})
    text(s, LEFT, 3.85, 7.5, 0.4, "ΔSharpe 為對 DL-THR 的差；H3 判準：零成本下差距縮小 ≥ 50%",
         size=12, color=GREY)
    callout(s, 8.30, 1.90, 4.30, 2.55, "這個分裂，比一致的結果更有訊息量",
            ["**v1／v2：輸在交易太頻繁**——拿掉成本，差距縮小八成以上。",
             "**v3：不是成本的問題**——零成本下它甚至**略優**（+0.195），成本一加回就翻負。"],
            style="light", size=13.5)
    callout(s, LEFT, 4.70, CONTENT_W, 1.60, "第一段的結論與它的邊界",
            ["**Q1 的答案是肯定的：動作空間是關鍵約束。**　但只在一個配對來源（GICS-SDP）、一個格點、"
             "108 期上做過；學習臂沒固定種子、每臂只跑一次（同組態重跑的差異可達 2.0–2.8 倍，"
             "小於效果量 2.1–4.1 倍）。零成本是「同一條政策在零成本下的績效」，不是「沒有成本會學出什麼政策」。"],
            style="plain", size=13.5)
    return """
H3 是機制：它輸，是不是因為換手成本？這邊的結果是分裂的，而我覺得這個分裂比一致的結果更有訊息量。

表上是它們對 DL-THR 的 Sharpe 差。v1 跟 v2 在含成本的時候差很多，但把成本拿掉重算，差距縮小了 87% 跟 83%——所以這兩代是輸在交易太頻繁。

但 v3 不一樣：拿掉成本之後，它對 DL-THR 反而從負 0.285 變成正 0.195，也就是在沒有成本的世界裡，它其實略優；是成本一加回來，它才翻負。所以 v3 的問題不是太常交易，而是它的那一點點增益，剛好不夠付它自己的換手成本。

所以「天天決定為什麼會輸」，在三代之間不是同一個原因。

下面是第一段的結論跟邊界：Q1 的答案是肯定的，動作空間確實是關鍵約束。但它只在一個配對來源、一個格點、108 期上做過，學習臂也沒有固定種子、每臂只跑一次。我測過同組態重跑的差異可以到 2 到 2.8 倍，還是小於效果量的 2.1 到 4.1 倍，所以結論成立，但範圍要講清楚。
"""


@reg(1.0, part=3)
def appendix_e(d):
    s = std(d, "第四部分 · 第一段延伸（探索性，未預先登記）　｜　附錄 E",
            "那換個演算法呢？結果還是一樣輸")
    text(s, LEFT, 1.80, 6.9, 0.7,
         "三代都用 LSTM 編碼、都是「擬合 Q 函數」這一族。會不會只是剛好挑錯演算法？"
         "我在同一批配對、同 108 期上換了四種做法：", size=14.5)
    # 2x2
    gx, gy, cw, ch = 2.25, 2.95, 2.35, 1.05
    text(s, gx, gy - 0.38, cw, 0.3, "Double-Q 目標", size=13, bold=True,
         color=NAVY, align="c")
    text(s, gx + cw + 0.1, gy - 0.38, cw, 0.3, "單網路 max 目標", size=13, bold=True,
         color=NAVY, align="c")
    text(s, LEFT, gy + 0.35, 1.45, 0.3, "LSTM 編碼", size=13, bold=True, color=NAVY)
    text(s, LEFT, gy + ch + 0.45, 1.45, 0.3, "MLP 編碼", size=13, bold=True,
         color=NAVY)
    cells = [(0, 0, "v3（官方）", "−0.323", NAVY), (0, 1, "v3s", "−0.422", NAVY),
             (1, 0, "v3m", "−1.528", ACCENT), (1, 1, "v3ms", "−1.301", ACCENT)]
    for r, c, name, val, col in cells:
        box(s, gx + c * (cw + 0.1), gy + r * (ch + 0.1), cw, ch, [
            {"text": name, "size": 12, "color": GREY, "after": 0, "align": "c"},
            {"text": val, "size": 24, "bold": True, "color": col, "align": "c"}],
            fill="F7F9FC", line=("D9DEE7", 1.0), anchor="m", inset=(0.05, 0.05))
    box(s, LEFT, gy + 2 * ch + 0.30, 6.9, 0.62, [
        {"text": "表格式價值迭代（vi）：無神經網路、無梯度 → ^^−2.134^^（九臂最差）",
         "size": 13.5}], fill="F7F9FC", line=("D9DEE7", 1.0), anchor="m")
    text(s, LEFT, 6.15, 6.9, 0.4,
         "數字為附錄 E 的重算 Sharpe（與 4.1.3 官方口徑差約 0.02–0.03，只用於排序）",
         size=11, color=GREY)
    callout(s, 7.90, 1.85, 4.70, 2.70, "看得出什麼",
            ["**編碼器才是主要變因**：MLP 兩格都差很多，進場數 808、824，逼近最愛交易的 v2（854）。",
             "**Double-Q 影響很小、方向不穩**：在 LSTM 上它比較好，在 MLP 上反而比較差。",
             "**連表格法都輸**：它收斂到精確解，排除了「優化沒調好」這個解釋。"],
            style="plain", size=13)
    callout(s, 7.90, 4.75, 4.70, 1.55, "證據等級要講清楚",
            ["未預先登記、每臂一次、無種子、無檢定——**只是探索性佐證**，不能單獨引用成統計結論。"],
            style="light", size=13)
    return """
講到這裡，一定會有人問：三代都用 LSTM、都是擬合 Q 函數這一族，會不會只是剛好挑錯演算法？

所以我後來又做了一個探索性的補充，放在附錄 E。在同一批配對、同樣 108 期上，換了四種做法：左上是官方的 v3，LSTM 加 Double-Q；右上把 Double-Q 拿掉；下排把 LSTM 換成沒有遞迴的 MLP；最下面是完全不同的一族——表格式價值迭代，沒有神經網路、沒有梯度下降。

結果：第一，編碼器才是主要變因，MLP 兩格都差很多，而且進場次數暴增到 800 多次，逼近最愛交易的 v2。第二，Double-Q 的影響很小，而且方向不穩定——在 LSTM 上它比較好，在 MLP 上反而比較差，這看起來比較像單次執行的雜訊。第三，最有說服力的是表格法：它收斂到精確解，不存在「訓練不夠」或「優化器沒調好」的問題，結果卻是九臂裡面最差的。

所以五種實作方式，全部輸給門檻選擇。這讓「只是挑錯演算法」這個質疑更難成立。

但證據等級我要講清楚：這部分沒有預先登記、每臂只跑一次、沒有種子也沒有檢定，只是探索性的佐證。我在探索過程中還踩過一次坑——在比較短的窗口上，去掉 Double-Q 看起來大勝，拉長到 108 期就反轉了。這個過程我也完整記在附錄 E。
"""


@reg(1.25, part=3)
def seg2(d):
    s = std(d, L2 + "§4.2", "第二段：讓模型逐對挑門檻，竟然輸給一個常數")
    text(s, LEFT, 1.80, CONTENT_W, 0.9, [
        "Q1 說「該把動作空間縮小」。那縮小之後還剩什麼能力？用監督回歸**逐對學門檻**，"
        "對照「**擴張窗最佳常數**」：每期只用前面的資料選**一個**最好的門檻，套用到所有配對——合法、無前視。"],
        size=15)
    table(s, LEFT, 2.85, [4.2, 2.6], [
        ["方法", "每期平均 capture_frac"],
        ["模型（30 維特徵 × 20 個動作）", "+0.005510"],
        ["擴張窗最佳常數（基準）", "**+0.010397**"],
        ["差", "−0.004887（p = 0.0004）"],
        ["現行 (2.0, 0.0)（附帶）", "+0.006932"],
        ["完美預知（天花板）", "+0.045794"],
    ], num_cols={1}, size=14, row_h=0.40, accent_cells={(3, 1)})
    text(s, LEFT, 5.40, 6.8, 0.8, [
        "capture_frac＝實際捕獲佔完美預知上限的比例。",
        "~~樣本 91,775 列、295 期；暖身 36 期、擴張窗、每 12 期重訓、6 期隔離帶。~~"],
        size=12.5, space_after=3)
    callout(s, 7.90, 2.85, 4.70, 2.45, "裁決：未過閘",
            ["模型只吃到天花板的 **−13.8%**（相對常數）。",
             "不只是沒學到東西——它**顯著劣於一個常數**。",
             {"text": "判準在執行前定稿。", "size": 12.5, "color": "C9D3E3"}],
            style="dark", size=14, heading_size=22)
    return """
第二段。第一段告訴我們：應該把動作空間縮小。那縮小到每期只選一次門檻之後，模型還剩什麼能力？

我用監督回歸去逐對學門檻，也就是讓模型看每一組配對的 30 維特徵，預測它該用 20 個動作裡的哪一個。對照組是「擴張窗最佳常數」：每一期只用截至前一期的資料，挑出一個整體最好的門檻，然後套用到所有配對——它完全不看個別配對，而且是合法、沒有前視的。

評估指標叫 capture_frac，就是實際賺到的，佔完美預知上限的比例。

結果：模型每期平均 0.0055，最佳常數是 0.0104，差距負 0.0049，p 值萬分之四。也就是說，模型不只是沒學到東西，它是顯著比一個常數還差。

判準在跑之前就定稿了，裁決是未過閘。
"""


@reg(1.25, part=3)
def argmax(d):
    s = std(d, L2 + "§4.2.1–4.2.3", "為什麼會輸？弱訊號遇上 argmax，雜訊被放大")
    text(s, LEFT, 1.80, CONTENT_W, 0.8, [
        "設計所依據的訊號：Spearman（價差波動度, 最佳進場門檻）＝ **+0.1007**（t ＝ 3.37）"
        "——統計上顯著，但**量級很弱**。"], size=15.5)
    callout(s, LEFT, 2.55, 5.75, 2.35, "機制：argmax 會挑到「誤差最大」的那個",
            ["選法是對 **20 個獨立回歸**的預測值取最大。",
             "訊號弱的時候，最容易被推到最高的，往往是**預測誤差最大**的那個輸出——"
             "選取本身帶進來的雜訊，比訊號還大。",
             {"text": "這是資訊科學的機制，不需要任何價格理論。", "size": 12.5,
              "color": GREY}], style="light", size=13.5)
    callout(s, 6.85, 2.55, 5.75, 2.35, "跟第一段是同一件事的兩面",
            ["天天決定的 DRL：輸出**塌成常數**（選的門檻中位 2.11–2.33，跟配對名次幾乎無關）。",
             "改用正確範式後：不是塌成常數，而是**比常數還差**。",
             "→ **逐對選門檻所需的訊號，在現有特徵裡不夠。**"], style="plain",
            size=13.5)
    callout(s, LEFT, 5.10, CONTENT_W, 1.25, "一個不是 AI 的副產品",
            ["擴張窗最佳常數比現行 (2.0, 0.0) **高 50%**，多數期選中 entry_z ＝ 3.0。"
             "但別外推：entry_z 的下界 **1.5 從沒跑過**，這只在跑過的取值上成立。"],
            style="plain", size=13.5)
    return """
為什麼會輸給常數？機制其實講得清楚，而且這是資訊科學的機制，不需要任何價格理論。

先看訊號：我設計的依據是價差波動度跟最佳進場門檻之間的相關，Spearman 是 0.10，t 值 3.37。統計上顯著，但量級很弱。

然後看選法：我是對 20 個獨立回歸的預測值取最大，也就是 argmax。問題就在這裡——當訊號很弱的時候，最容易被推到最高的，往往是預測誤差最大的那一個輸出。所以選取這個動作本身帶進來的雜訊，比訊號還大。

右邊是我覺得很漂亮的一點：這跟第一段是同一件事的兩面。天天決定的 DRL，輸出會塌成一個常數，它選的門檻中位數是 2.11 到 2.33，跟配對排名幾乎沒有關係——雜訊太大的時候，最安全的預測就是常數。而我改用正確的範式之後，結果不是塌成常數，而是比常數還差。兩者一致指向：逐對選門檻所需要的訊號，在現有的特徵裡就是不夠。

最後有一個不是 AI 的副產品：那個擴張窗最佳常數，比現行的 2.0 進場高了 50%，多數期都選中 3.0。但我要提醒不能外推——進場門檻的下界 1.5 我從來沒跑過，所以這只在跑過的範圍內成立。
"""


@reg(1.5, part=3)
def seg3(d):
    s = std(d, L3 + "§4.3", "第三段：跟固定門檻比，五個來源方向全部是正的")
    text(s, LEFT, 1.78, CONTENT_W, 0.4,
         "固定形成期配對，只換交易端：Δ ＝ DL-THR − Z-Score（15 格等權、逐日報酬差、區塊拔靴）",
         size=14, color=GREY)
    table(s, LEFT, 2.20, [2.4, 1.6, 1.0, 1.1, 2.6, 1.2, 1.5], [
        ["配對來源", "年化Δ（pp）", "IR", "勝日%", "95% CI（pp）", "p", "BH 校正 p"],
        ["GICS-SSD", "+0.798", "0.505", "51.3", "[+0.165, +1.689]", "0.0403", "0.0504"],
        ["HDBSCAN", "+0.666", "0.493", "52.1", "[+0.265, +1.148]", "0.0042", "0.0210"],
        ["GICS-SDP", "+0.579", "0.429", "51.8", "[+0.170, +1.046]", "0.0111", "0.0278"],
        ["K-means", "+0.310", "0.267", "51.6", "[+0.027, +0.585]", "0.0297", "0.0495"],
        ["Agglomerative", "+0.239", "0.189", "51.5", "[−0.091, +0.606]", "0.1802", "0.1802"],
    ], num_cols={1, 2, 3, 5, 6}, center_cols={4}, size=14, row_h=0.40,
        bold_cells={(2, 6), (3, 6), (4, 6)})
    callout(s, LEFT, 4.80, 5.75, 1.55, "不要用「顯著／不顯著」二分法讀這張表",
            ["K-means 0.0495 過、GICS-SSD 0.0504 沒過——**只差 0.0009**。應該看效果量跟區間："
             "四個來源的下界都是正的。"], style="light", size=13.5)
    callout(s, 6.85, 4.80, 5.75, 1.55, "增益最大的是傳統 GICS",
            ["不是任何一種資料驅動分群。→ 門檻選擇的價值**跟分群方法無關**，"
             "它來自交易端的設計，不是形成期的聰明。"], style="plain", size=13.5)
    return """
第三段是主檢定，也是全篇唯一通過多重檢定校正的正向結果。再提醒一次證據等級：這一段的判準不是事前登記的，是依文獻慣例在分析時採用的。

做法是：形成期的配對完全固定，只把交易端從固定門檻換成 DL-THR，看逐日報酬差。五個配對來源，年化增益從 0.24 到 0.80 個百分點，方向全部是正的；BH 校正後，五個裡面有三個顯著。

左下請容我強調：不要用二分法讀這張表。K-means 校正後 p 是 0.0495，通過；GICS-SSD 是 0.0504，沒通過——兩個只差 0.0009。在門檻邊上，過或不過非常不穩，應該看效果量跟信賴區間：五個裡面有四個，信賴區間的下界是正的。

右下是我覺得最值得講的：增益最大的，是傳統的 GICS 產業分類，不是任何一種資料驅動的分群。這說明門檻選擇的價值跟分群方法無關——它來自交易端的設計，而不是形成期挑配對挑得比較聰明。

還有一個背景我要交代：這組顯著是在我修正了一個對沖口徑的實作錯誤之後才出現的，修正前是零比五。這件事記在附錄 B。
"""


@reg(1.0, part=3)
def equity(d):
    s = std(d, L3 + "§4.3、§5.1.1", "25 年權益曲線：差距看得到，問題也看得到")
    line_chart(s, LEFT, 1.75, 8.3, 4.55, YEARS,
               [("＋DL-THR 門檻選擇", EQ_DL), ("Z-Score 固定門檻", EQ_ZS)],
               [ACCENT, BLUE], y_min=9000, y_max=15000)
    stat(s, 9.30, 1.95, 3.3, "13,062", "＋DL-THR 25 年終值", color=ACCENT)
    stat(s, 9.30, 3.25, 3.3, "11,072", "Z-Score 25 年終值", color=BLUE)
    callout(s, 9.30, 4.45, 3.30, 1.90, "起始資本 10,000",
            ["GICS-SSD，15 個配置**等權**，不是挑出來的最佳格。",
             "注意：增長**大多發生在 2008–2009**。"], style="plain", size=13)
    return """
這是把門檻選擇疊加在 GICS-SSD 上的 25 年權益曲線，起始資本一萬。

紅線是加了 DL-THR 的，藍線是固定門檻。25 年終值分別是 13,062 跟 11,072。差距是看得到的。

但有兩件事要講。第一，這是 15 個參數配置的等權組合，不是我挑出來的最佳格。第二，請大家看曲線的形狀：大部分的增長發生在 2008 到 2009 年，之後兩條線都走得很平，甚至慢慢往下。這一點在第五部分會變成一個重要的限制——獲利高度集中在特定的市場環境。

而且這張圖也預告了下一個問題：25 年從一萬變一萬三，年化大約 1%。光是這個數字，就已經說明了為什麼我不能宣稱它可以交易。不過在講那個之前，我想先檢查：這個增益到底是不是真的「學到東西」？
"""


@reg(1.25, part=3)
def fake_gains(d):
    s = std(d, LX + "§4.4", "這個增益是真的學到東西嗎？我排除三種「假增益」")
    tests = [
        ("① SKIP 選股", "底層期望為負時，**隨便跳過都會避損**", "置換檢定：隨機跳過 2,000 次"),
        ("② 門檻水準", "DL-THR 選的門檻中位 2.19–2.37，**本來就比 2.0 高**", "把 Z-Score 門檻拉到同水準對照"),
        ("③ 總曝險", "**少交易本身**會不會就是增益？", "固定 2.0、隨機跳過同數量配對期"),
    ]
    x = LEFT
    for head, why, how in tests:
        box(s, x, 1.78, 3.8, 1.48, [
            {"text": head, "size": 15, "bold": True, "color": NAVY, "after": 3},
            {"text": why, "size": 12.5, "after": 2},
            {"text": how, "size": 11.5, "color": GREY}], fill="F7F9FC",
            line=("D9DEE7", 1.0))
        x += 4.05
    table(s, LEFT, 3.42, [2.1, 2.3, 2.1, 2.3, 3.1], [
        ["配對來源", "① SKIP 選股", "② 門檻水準", "③ 總曝險", "判定"],
        ["GICS-SSD", "排除（3/15）", "排除（4–15%）", "排除（11/15）", "三項全排除"],
        ["HDBSCAN", "排除（3/15）", "排除（8–14%）", "排除（11/15）", "三項全排除"],
        ["GICS-SDP", "排除（2/15）", "排除（−3–7%）", "排除（9/15）", "三項全排除"],
        ["K-means", "否證（技巧 −220）", "排除（19–32%）", "部分（7/15）", "SKIP 沒有技巧"],
        ["Agglomerative", "否證（技巧 −102）", "未排除（38–75%）", "部分（6/15）", "多可由調高門檻解釋"],
    ], size=13, row_h=0.38, accent_cells={(4, 1), (5, 1), (5, 2)},
        bold_cells={(1, 4), (2, 4), (3, 4)})
    callout(s, LEFT, 5.82, CONTENT_W, 0.68, None,
            [{"text": "「機制證據」跟「統計顯著」的交集只有 **HDBSCAN、GICS-SDP**。"
                      "~~三項都是事後重抽，不含「跳過 A 就把錢給 B」的槽位再配置效應。~~",
              "size": 13.5}], style="plain")
    return """
第三段看起來有增益，但我要先檢查：這個增益，會不會根本不是「學到東西」，而是某種機械效應？我想到三種「假增益」，逐一排除。

第一種是 SKIP。DL-THR 可以拒絕交易，實測會跳過三到四成的配對期。但如果底層策略的期望值本來就是負的，那隨便跳過任何一批配對，在期望上都會「避開損失」。所以我做置換檢定：隨機跳過同樣數量，重抽兩千次，看它有沒有贏過隨機。

第二種是門檻水準：DL-THR 選的門檻中位數是 2.19 到 2.37，本來就比 2.0 高。會不會只是「把門檻調高」就有效？我把固定門檻拉到同樣水準來對照。

第三種是總曝險：少交易本身會不會就是增益？我固定門檻 2.0、隨機跳過同樣數量的配對期來對照。

結果在表上：兩條 GICS 跟 HDBSCAN，三項都排除了。但 K-means 跟 Agglomerative 不行——它們的 SKIP 技巧成分是負的，也就是它們跳過的配對，還不如隨機跳過；Agglomerative 的增益，多半就是調高門檻的效果。

所以如果把「機制證據」跟上一張的「統計顯著」取交集，只剩 HDBSCAN 跟 GICS-SDP。另外這三個檢定都是事後重抽，沒有辦法涵蓋「跳過 A、把錢給 B」這種再配置效應，這個列為後續研究。
"""


@reg(1.0, part=3)
def level_tension(d):
    s = std(d, LX + "§4.4（替代解釋二）、§3.7", "會不會只是門檻調高而已？只能複製 3.6–32%")
    table(s, LEFT, 1.90, [2.3, 2.2, 2.6, 2.6, 2.2], [
        ["配對來源", "A：DRL − ZS(2.0)", "B：DRL − ZS(同門檻)", "C：純門檻管道", "複製率"],
        ["GICS-SSD", "+0.798", "+0.676 ~ +0.769", "+0.029 ~ +0.122", "3.6 ~ 15.3%"],
        ["GICS-SDP", "+0.579", "+0.537 ~ +0.594", "−0.015 ~ +0.042", "−2.6 ~ 7.3%"],
        ["HDBSCAN", "+0.666", "+0.576 ~ +0.611", "+0.055 ~ +0.090", "8.3 ~ 13.5%"],
        ["K-means", "+0.310", "+0.210 ~ +0.250", "+0.060 ~ +0.100", "19.4 ~ 32.3%"],
        ["Agglomerative", "+0.239", "+0.061 ~ +0.149", "+0.091 ~ +0.178", "38.1 ~ 74.5%"],
    ], num_cols={1, 2, 3, 4}, size=13.5, row_h=0.40, hl_rows={5})
    text(s, LEFT, 4.40, CONTENT_W, 0.4, "單位：年化 pp；ZS(同門檻)＝把 Z-Score 門檻拉到 2.1–2.3",
         size=11.5, color=GREY)
    callout(s, LEFT, 4.85, 5.75, 1.45, "水準不是唯一來源，但也不是無關",
            ["如果增益純粹來自水準，拉高門檻應該複製**全部**；實測四個來源只複製 3.6–32%。"
             "但 **Agglomerative 例外**：純門檻管道自己就顯著（p = 0.019）。"], style="light",
            size=13)
    callout(s, 6.85, 4.85, 5.75, 1.45, "覆蓋範圍要講清楚",
            ["2.1–2.3 覆蓋五個來源、2.5／3.0 只覆蓋四個，**下界 1.5 從沒執行**。"
             "只能說「在跑過的取值上，水準複製不了多數增益」。"], style="plain", size=13)
    return """
第二種假增益我特別拿出來講，因為它是這篇研究一個不可迴避的張力。

A 欄是 DL-THR 對原本 2.0 門檻的增益；B 欄是對「拉到同樣門檻水準」的 Z-Score 的增益；C 欄就是純粹調高門檻本身帶來的效果。複製率就是 C 佔 A 的比例。

在四個來源上，把門檻調高只能複製 3.6% 到 32% 的增益，B 欄的效果量幾乎沒縮水。所以增益不是單純調高門檻。

但 Agglomerative 是例外，也就是反紅那一列：它的複製率達到 38% 到 75%，而且純門檻管道本身就顯著。換句話說，Agglomerative 那一點點增益，多半就是門檻調高的效果。

右下是覆蓋範圍的限制：門檻 2.1 到 2.3 覆蓋了五個來源、2.5 跟 3.0 只覆蓋四個，而下界 1.5 從來沒執行過。所以我只能說：在跑過的取值上，水準複製不了多數的增益。我不能說最適門檻在哪裡，也不能說這個維度已經窮盡了。
"""


@reg(1.0, part=3)
def counterfactual(d):
    s = std(d, LX + "§4.5", "全資訊標籤有幫上忙嗎？資料說：沒有")
    table(s, LEFT, 1.85, [4.25, 1.45, 2.05, 0.9], [
        ["對照（Agglomerative-SSD，15 格等權）", "年化Δ（pp）", "95% CI（pp）", "p"],
        ["DL-THR − Z-Score", "+0.239", "[−0.09, +0.61]", "0.180"],
        ["RL-THR（ε=0.05）− Z-Score", "+0.528", "[+0.01, +1.28]", "0.092"],
        ["RL-THR（ε=0.10）− Z-Score", "+0.479", "[−0.03, +1.23]", "0.130"],
        ["RL-THR（ε=0.20→0.02）− Z-Score", "+0.519", "[+0.05, +1.20]", "0.071"],
        ["RL-THR（ε=0.05）− DL-THR", "+0.288", "[−0.13, +0.83]", "0.231"],
        ["RL-THR（ε=0.10）− DL-THR", "+0.240", "[−0.18, +0.78]", "0.326"],
        ["RL-THR（ε=0.20→0.02）− DL-THR", "+0.279", "[−0.10, +0.74]", "0.192"],
    ], num_cols={1, 2, 3}, size=13, row_h=0.37, hl_rows={5, 6, 7})
    callout(s, 9.60, 1.85, 3.00, 2.95, "三個 RL 版本的點估計都比全資訊版好",
            ["+0.24～+0.29 pp", "但全部**不顯著**（p＝0.19–0.33）"], style="plain",
            size=13, heading_size=14)
    callout(s, LEFT, 5.05, CONTENT_W, 1.30, "所以我不主張「部分回饋比較好」，只主張否定的那一半",
            ["**資料不支持「全資訊標籤是效果來源」這個預期**——如果它是，DL-THR 應該贏過三個 RL 版本，實測方向相反。"
             "可能的機制是過度擬合那 8 個沒被執行的動作（推測，未檢定）。~~限制：只在一個來源做、RL-THR 沒有多輪重訓。~~"],
            style="light", size=13.5)
    return """
第三部分我留了一個伏筆：全資訊的 DL-THR，跟只看得到自己選的那個動作的 RL-THR，誰比較好？

直覺上應該是全資訊比較好，因為每次決策有九筆標籤。但結果是：三個 RL 版本，不管探索率設多少，點估計全部比全資訊版好，大約好 0.24 到 0.29 個百分點。

不過差異全部不顯著，p 值在 0.19 到 0.33 之間，信賴區間都涵蓋零。所以我不主張「部分回饋比較好」。我只主張否定的那一半：資料不支持「全資訊標籤是這個交易端效果的來源」這個預期——如果它是，DL-THR 應該要贏，結果方向是相反的。

一個可能的機制是過度擬合：全資訊訓練要求網路同時擬合九個動作的報酬，其中八個在那次決策裡根本沒被執行，如果那些反事實估計本身很吵，硬去擬合它們反而會傷害真正被執行的那個動作。但這是推測，我沒有檢定。

限制是：這個對照只在一個配對來源上做，而且 RL-THR 沒有做多輪重訓。
"""


@reg(1.25, part=3)
def robustness(d):
    s = std(d, LX + "§4.6", "穩健性：重訓五次、分市況、改成本")
    cols = [
        ("① 五輪獨立重訓", "修正實作錯誤後的績效位移，會不會只是重訓雜訊？",
         ["修正前 Sharpe 低於修正後四輪最小值：**88／120（73.3%）**",
          "二項檢定 p ＝ 3.7 × 10⁻³⁶",
          "跨輪標準差中位 **0.031**，遠小於位移",
          "~~例外：KM-SSD-DRL 6/15（p＝0.061），不可單獨宣稱~~"]),
        ("② 市況分層（Regime）", "什麼時候賺錢？",
         ["十列全部同一型態：**動盪期為正**，平靜與一般期為負或零",
          "DL-THR 在 30 格中**改善 22 格**，集中在虧最多的兩檔",
          "~~但動盪期報酬 54–87% 集中在 2008–2009 兩年~~"]),
        ("③ 交易成本", "成本假設換一下會怎樣？",
         ["五個來源的 DL-THR **都提高 break-even**，幅度 8.3–31.9 bps",
          "GICS＋DL-THR 的 break-even 達 **0.99–1.01%**（成本 0.58%）",
          "~~K-means 兩臂餘裕為負：現行成本下不可行~~"]),
    ]
    x = LEFT
    for head, q, body in cols:
        callout(s, x, 1.85, 3.8, 4.45, head,
                [{"text": q, "size": 13, "color": GREY, "after": 8}] + body,
                style="plain", size=13.5, heading_size=16)
        x += 4.05
    return """
最後是穩健性，三件事。

第一，五輪獨立重訓。我在研究後期修正了一個對沖口徑的實作錯誤，修正後績效有明顯的位移；但網路沒有固定種子，所以要確認這個位移不是重訓的雜訊。我用一個獨立預先註冊的判準檢查：修正前的 Sharpe，低於修正後四輪最小值的比例，是 120 組裡的 88 組，73%。如果沒有效果，這個比例應該是五分之一，二項檢定的 p 值是 10 的負 36 次方。跨輪的標準差中位數只有 0.031，遠小於位移。唯一的例外是 K-means-SSD 那條臂，它不能單獨宣稱。

第二，市況分層：把交易日依市場波動分成三檔。十列全部是同一個型態——動盪期賺錢，平靜期跟一般期是虧的或持平。DL-THR 在 30 格裡改善了 22 格，改善集中在原本虧最多的兩檔。但是動盪期的報酬，有 54% 到 87% 集中在 2008 到 2009 這兩年，所以這不能拿來建立「只在高波動期交易」的策略。這部分的細節在附錄 C。

第三，交易成本：五個來源的 DL-THR 都提高了損益兩平的成本，GICS 加 DL-THR 可以撐到大約 1%，對 0.58% 的成本有七成以上的餘裕；但 K-means 兩臂的餘裕是負的。
"""


@reg(1.0, part=3)
def three_answers(d):
    s = d.slide(dark=True)
    from lib import header, title, page_number
    header(s, "第四部分 · 小結　｜　§4.7", dark=True)
    title(s, "三個問題，三個不一致的答案", dark=True)
    rows = [("Q1", "動作空間是關鍵約束嗎？", "是", ACCENT, "完整預先登記",
             "換手 2.1–4.1 倍，3/3 達標"),
            ("Q2", "逐對辨識能力存在嗎？", "否", WHITE, "跑前定稿",
             "顯著輸給最佳常數，p = 0.0004"),
            ("Q3", "相對固定門檻有效嗎？", "是，但不可交易", WHITE, "分析時判準",
             "方向 5/5 為正，BH 校正後 3/5")]
    y = 1.95
    for q, ask, ans, col, grade, why in rows:
        text(s, 0.80, y, 1.0, 0.7, q, size=30, color=WHITE)
        text(s, 2.0, y + 0.08, 4.8, 0.9, [ask, {"text": why, "size": 13,
                                                "color": "9FB3D1"}], size=19,
             color=WHITE)
        text(s, 6.9, y + 0.05, 3.3, 0.7, ans, size=24, bold=True, color=col)
        text(s, 10.3, y + 0.15, 2.4, 0.6, grade, size=13, color=LIGHT)
        y += 1.22
    text(s, 0.80, 5.75, 11.8, 0.8, [
        "合起來：**門檻選擇的增益來自動作空間的設計，不來自逐對的辨識能力。**"],
        size=19, color=WHITE)
    page_number(s, d.n, dark=True)
    return """
把第四部分收起來。

Q1：動作空間是關鍵約束嗎？是。天天決定會讓換手率變成兩到四倍，三條臂全部達標，這是完整預先登記的結果。

Q2：模型能逐對辨識該用哪個門檻嗎？否。它顯著輸給一個常數，這是跑前定稿的判準。

Q3：門檻選擇相對固定門檻有沒有用？有，方向五個來源全正、校正後三個顯著——但不可交易。這是分析時的判準。

請注意最右邊那一欄，三段的證據等級不一樣，我刻意標出來，因為我認為把預先登記的結果跟事後判準的結果混在一起報告是不誠實的。

這三個答案並不一致，但合起來其實講的是同一件事：門檻選擇的增益，來自動作空間的設計，而不是模型逐對辨識的能力。如果三個答案全部都是「是」，我反而會懷疑。

那 Q3 的「不可交易」是怎麼來的？這是第五部分。
"""
