# 核心數據處理與計算
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.11.0
matplotlib>=3.7.0

# 數據源與網絡爬蟲
requests>=2.31.0
yfinance>=0.2.36            # 獲取股價數據 (需新版以修復 API 變動)
lxml>=5.0.0                 # 用於 pd.read_html 解析 Wikipedia 表格
html5lib>=1.1               # 用於 pd.read_html 的備用解析器
beautifulsoup4>=4.12.0      # 網頁解析依賴

# 存儲格式支持
pyarrow>=14.0.0             # 讀寫 Parquet 文件的高效引擎 (必需)

# 機器學習與統計分析
scikit-learn>=1.3.0         # PCA, HDBSCAN, StandardScaler
statsmodels>=0.14.0         # adfuller, coint_johansen, OLS
joblib>=1.3.0               # 並行處理 (Parallel processing)
hdbscan>=0.8.33             # HDBSCAN 聚類
umap-learn>=0.5.3           # UMAP 降維

# 深度強化學習
torch>=2.0.0                # DRL 代理神經網路
gymnasium>=0.26.0           # 強化學習環境接口
stable-baselines3>=2.0.0    # 強化學習框架

# 時間序列距離
dtaidistance>=2.3.0         # DTW 距離計算

# 績效可視化與簡報工具
streamlit>=1.30.0           # 績效比對 Dashboard 框架
plotly>=5.18.0              # 互動式圖表繪製
psutil>=5.9.0               # 系統記憶體監控 (防止 OOM)
